	публичная  функция  linearEquation ( $ a , $ b ) {

			if ( $ a ! = 0 ) {
				MyLog :: log ( «Это линейное уравнение» );
				$ this -> x = ( $ b * (- 1 )) / $ a ;
				вернуть  $ this -> x ;
			}
			} 

			бросить  новое  RuntimeException ( «Такое уравнение не существует \ n» );

	}
}
